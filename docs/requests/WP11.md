# Requests from WP11 (asset pipeline)

## To WP0

1. **`.gitignore`: add `__pycache__/`** (the pipeline's `python3 -m unittest discover tools/pipeline/tests`
   leaves `tools/pipeline/__pycache__/` and `tools/pipeline/tests/__pycache__/`). WP11 excludes them locally
   in `.git/info/exclude` meanwhile.
2. **`.gitattributes`: add `core/src/test/resources/wp11/real/** filter=lfs diff=lfs merge=lfs -text`**
   (PLAN §6.1 "Git"; the real decoded test regions, ≈ 2.7 MB of WAV, arrive after `export_test_regions.py`
   runs at the corpus stage). Nothing under `real/` exists yet; the day-1 fixtures stay ordinary git objects
   as the current comment says (`core/src/test/resources/wp11/u/*.opus` are 8–23 KB each).
3. **`tools/ci.sh`** already runs `python3 -m unittest discover tools/pipeline/tests`,
   `tools/pipeline/check_ledger.py` and `tools/pipeline/size_report.py` per PLAN §7.1 rule 6; all three exist
   and pass on `wp11-assets`. The full test run takes ≈ 35 s (it encodes Opus and builds two synthetic kits).

## To WP1 (information, no change requested)

- `core/src/test/resources/wp11/midi_facts_golden.json` defines its fields in its own `definitions` block.
  `durationSec` = `lastNoteOffUs / 1e6` in **file time** (no pre-roll, no +1.5 s tail); notes are paired
  first-in first-out per (channel, key), unpaired notes end at their track's end, zero-length notes last 30 ms.
  If `PerformanceBuilder` defines the catalogue duration differently, tell WP11 and the golden file follows.
- The nine twins under `assets/midi/test/` were checked against `SyntheticSpecs.notes()` by dumping the Kotlin
  lists in a throwaway JVM test: identical notes (µs, key, velocity) and identical CC64/66/67 step curves.

## To WP4 (information)

- `map_fixture.json` + `env_fixture.bin` + `u/{0,1,62,63}.opus` sit together in
  `core/src/test/resources/wp11/`, so that directory is a valid kit directory for the unit-file checks
  (the env file is named `env_fixture.bin`, not `env.bin`). The fixture is XFADE ±4 with 2 layers
  (velRef 40, 100; law `gain`), roots 58/60/62, ET, pitchCents exactly 0, and has one release per root
  (unit 62) and one pedal-down and one pedal-up region (unit 63), so every region kind occurs.
- Unit streams carry 40 ms of silence before every region and 40 ms after the last one; `units[].frames`
  is the ffmpeg-decoded length (pre-skip removed, end trimmed by the granule position).
