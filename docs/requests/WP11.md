# Requests from WP11 (asset pipeline)

## To WP0

1. **`.gitignore`: add `__pycache__/`** (the pipeline's `python3 -m unittest discover tools/pipeline/tests`
   leaves `tools/pipeline/__pycache__/` and `tools/pipeline/tests/__pycache__/`). WP11 excludes them locally
   in `.git/info/exclude` meanwhile. (`build/` is already ignored.)
2. ~~LFS line for `core/src/test/resources/wp11/real/**`~~ **withdrawn**: the seven real regions and
   `regions.json` (2.6 MB, each WAV's SHA-1 recorded in `regions.json`) are committed on `wp11-assets` as
   ordinary binary git objects (`*.wav binary`), like the other wp11 fixtures, so every clone and the
   integration merge carry them; `tools/pipeline/tests/test_tracked.py` checks they are tracked and match.
3. The kit binaries (grand 69 MB, upright 12 MB, harpsichord 5 MB) are committed on `wp11-assets` through the
   existing `app/src/main/assets/instruments/**/*.opus|*.bin` LFS lines (no plain-git blobs). Nothing is
   pushed; the integrator's merge carries the LFS objects from the shared local store.
4. **`tools/ci.sh`** runs `python3 -m unittest discover tools/pipeline/tests`, `tools/pipeline/check_ledger.py`
   and `tools/pipeline/size_report.py` per PLAN §7.1 rule 6; all three pass on `wp11-assets` (61 tests, ≈ 25 s).

## To WP4 (information)

- Kit directories are named by instrument (`instruments/grand/`, not `instruments/grand-hd/`); the `kit`
  field inside `map.json` says `grand-hd`.
- Unit streams carry 40 ms of silence before every region and 40 ms after the last one; `units[].frames`
  is the ffmpeg-decoded length (pre-skip removed, end trimmed by the granule position).
- Salamander's C8 recordings sound ≈ +95 cents (measured on every layer); `pitchCents` carries it and the
  KeyMap corrects it (PLAN §3.5). Upright top notes are +35..+55 cents. Both are real, not fit errors.
- The fixture notes of stage 1 still hold (`map_fixture.json` + `env_fixture.bin` + `u/{0,1,62,63}.opus`).

## To WP1 (information, no change requested)

- `midi_facts_golden.json` now covers 210 files (197 catalogue movements + 13 test files). Definitions in
  its own `definitions` block; `durationSec` = `lastNoteOffUs / 1e6` in file time.

## To WP9 (information)

- `catalog.json` omits empty shelves (Handel's shelf disappears while HWV 430 is not installed) and drops
  absent works from `startHere` (12 movements, not 13). `optionalWorks` exists only in the source.
- Sources: `krueger`, `sankey`, `mutopia`, `bednarek`, `nieb`, `frantz`. Work-level `tier` "C" on
  `mozart.k457` and `beethoven.op111-1` (listed in `build/listen.txt` for L-6).
- Multi-piece single files: the Goldberg Variations, the suites/partitas, BWV 971/974/903, Mozart K. 281 and
  Beethoven op. 126 are one movement each (Sankey and Bednarek ship one file per work). "Start here:
  Goldberg Aria" is therefore `bach.bwv988.1`, the whole work.

## To WP0 (added in stage 2)

5. ~~`.gitattributes` line for SANKEY.txt~~ **done locally**: WP11 added a per-directory
   `app/src/main/assets/licenses/.gitattributes` (`SANKEY.txt -text`; AGP ignores dotfiles in assets) and
   committed `SANKEY.txt` byte-identical with its CRLF ends. WP0 may fold the line into the root
   `.gitattributes` and delete the nested file; `test_tracked.py` checks the attribute either way.

## To the plan owner (section 6.10)

6. **MIDI budget line: 3.5 MB -> 4.7 MB** (measured, 197 catalogue movements + 13 test files + the golden
   facts). The total stays under the 100 MB cap (82.2 MB with the 4 MB code estimate). `size_report.py`
   acknowledges up to 5.0 MB for this line (`ACKNOWLEDGED`) and fails any other line more than 25 % over.

## Integrator answers (M1, 2026-09-22)
- 1: `__pycache__/` added to `.gitignore`. 2–4: merged as described; CI runs the pipeline tests, the ledger
  and the size report (70 tests).
- `smf_stats.py` pedalMode changed at integration: SWITCH needs a CC64 value > 0 (bach_847/850 carry only a
  CC64 = 0 and are NONE, matching WP1's builder); `midi_facts_golden.json` regenerated with the script and
  `catalog.json` updated for those two movements. Please regenerate `catalog.json` from the script on your
  next pipeline run so the two agree byte for byte.
