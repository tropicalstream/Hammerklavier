# Requests from WP12 (session orchestration)

1. **WP0 (AppController, Wiring):** construct `session.SessionController` with the frozen §2.3
   signature, then `start()`; forward `UiAction`s to `onAction`, CONTROL `--el seek` to
   `controlSeekDisplayMs`, `--es play` to `controlPlay`, ThermalGovernor levels to
   `onThermalLevel`, EngineBench's cap to `setQ0Cap`, leaving the app to `onAppLeft`, the 1 Hz HUD
   tick to `tick()`; draw `facts()`. Set the hooks `onUiEvent` (→ `UiStateMachine.onEvent`),
   `onBrightnessCap`, `onLeave`, `onRotateToken`, and the fields `companionUrl`,
   `companionToken`, `version`, `batteryTenths`. Pass the controller to CompanionServer as its
   `CompanionCommands`. Settings keys used are listed in `session/SessionSettings.kt`
   (`SessionKeys`); the typed Settings should persist them as-is.
2. **WP10 (UiText) / WP0:** set `SessionController.statusPriority = UiText::priority` (the
   §1.8 fallback table in `StatusBoard.defaultPriority` is used until then).
3. **WP9 / WP10:** the two computed shelves use the shelf ids `SessionController.SHELF_START_HERE`
   (`"start-here"`) and `SessionController.SHELF_RECENT` (`"recent"`) in `UiAction.Play`.
   Please use these strings (or propose others and I will change the constants).
4. **WP9 (CompanionServer):** convert `?ms=` with `session.SeekUnits.displayMsToSongUs` so T12.6
   holds for the companion path.
5. **WP3 (for T12.8 at M5):** a pre-limiter peak tap on `MasterProcessor` (or a DspSet whose
   master can be bypassed) so `LevelCalibrationTest` can measure before the limiter.
