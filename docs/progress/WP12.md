# WP12 progress (session orchestration, pure)

Branch `wp12-session` from `contracts-v1`. Plan: §2.6, §4.9, §1.5, §5.6 (listeners), §7.2 WP12.

## Done
- `session/SessionController.kt`: frozen constructor; implements `CompanionCommands` and
  `AudioListener`. Playback with a monotone generation counter, stale-result dropping, next-item
  pre-compilation; playlists from the chosen shelf (movement → rest of work → later works of the
  shelf; Start here and Recently played play their lists); Previous > 3 s restarts; end of
  movement → next; instrument switch pause(30) → kit → KeyMap (HKLoader) → setBank →
  setPerformance(−1, wasPlaying) → room, renderer never awaited; per-work instrument memory;
  `velocityPolicy: flat` → 72 on pianos; view → exactly one setRoom; thermal → QualityLadder,
  Q3 display rest (REST_ON/OFF, persistent status); sync test (SYNC_CLICK + sync flash, restores
  the movement paused); seek conversion; resume point (5 s, pause, leave) and recently played
  (≤ 15); tuning → setKeyMap only; registration, tempo, mix, sight, presence floor, A/V lead per
  route; new-BT status once per device; rescan statuses; 1 Hz `nowPlayingJson` (display time).
- `session/StatusBoard.kt`, `session/FactsAssembler.kt` (+ `SeekUnits`), `session/ListenerRooms.kt`,
  `session/SessionSettings.kt` (`SessionKeys`, `ResumePoint`, typed settings).
- Tests (`core/src/test/.../session/`): T12.1–T12.7 in `SessionControllerTest` (24),
  `StatusBoardTest` (4), `ListenerRoomsTest` (5), `SessionSettingsTest` (2),
  `LevelCalibrationTest` (probe exercised on SineCore; the real T12.8 is @Ignore until M5).

## Remaining
- T12.8 at M5: needs WP2 EngineCore, WP3 DspFactory/RoomAcoustics + pre-limiter tap, WP11 exported
  regions; then write the measured `masterDb` into `SessionKeys.DEFAULT_MASTER_DB`.

## Decisions / deviations
- Shelf ids for computed shelves: `"start-here"`, `"recent"` (plan leaves them unnamed; request filed).
- Hooks (`onUiEvent`, `onBrightnessCap`, `onLeave`, `onRotateToken`, `nanoTime`, `statusPriority`)
  are properties, since the frozen constructor has no UI or window parameters.
- The listener comes from `InstrumentAnchors.listener` (WP7); `ListenerRooms.table` holds the §5.6
  table as the fallback and test oracle.
- Old kits are not `release`d on a switch (switching back stays instant); revisit with WP4 at M3.
- `Work.tuning` is not applied automatically; the per-instrument user tuning is used.
- Compile failures of library movements post `IMPORT_FAILED(title, reason, detail)`.
- No WP0 contracts-v1.1 body or WP11 fixture was needed: every test runs on the v1 stubs.

## Merge 2026-09-22
- Merged main (contracts-v1.1, WP11 fixtures, WP7 MeshBuilder): no conflicts; all session tests green.
- `docs/wiring/WP12.md` written.
- T12.8 stays @Ignore: WP2 EngineCore, WP3 DspFactory/RoomAcoustics/MasterProcessor tap and the WP11
  exported regions are not on main; it is an M5 integration test by plan (§3.13, §7.2, R101).

## Test results
- `tools/gw :core:test` green after merging main (2026-09-22); session: 37 tests, 0 failures, 1 skipped (T12.8).
- `tools/check_purity.sh`: OK.
