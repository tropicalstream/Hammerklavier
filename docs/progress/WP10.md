# WP10 progress (UI and overlays), branch wp10-ui

## Done
- core `ui/model`: `UiText` (every word; StatusCode/RejectReason/FallbackReason/PerfWarning text and
  priority, lower number wins), `MenuTree` (Transport, Instrument, Library/shelves/works, More, Sound,
  Sight, Calibrate and every option list), `HudModel` (HUD strings, pills, credit, title card, and the
  concrete `UiOverlayState`), `UiStateMachineImpl` (the §1.3 table, menus with paging by 7, Adjust,
  cards, panels, HUD 6 s, credit 8 s, toast 1.5 s, hint for sessions 1–3, rest).
- app `ui/`: `OverlayViews` (OverlayHost), `TitleCardView`, `HudView`, `MenuCardView`, `CardViews`
  (floor swatches, sync), `PanelView`, `Styles`.
- Tests (JVM): GestureTableTest (T10.1, 56 cells) + GestureRulesTest, MenuTest (T10.2),
  FirstRunTest (T10.3), HudTest (T10.4), CardsTest (T10.5). Nothing needed contracts-v1.1 or WP11
  fixtures; no @Ignore.

## Decisions / deviations
- Adjust Tempo and the A/V sync card preview live (SetTempo / SetAvLead on every step) and restore
  the original on cancel; Position only seeks on tap. Display floor stores only on tap.
- Import panel: tap = Rotate token (the panel's one row); double-tap/back closes.
- Transport rows Play/Next/Previous keep the menu open; instrument choice and Play close menus;
  option rows go back one level; closing a card/panel/adjust returns to the menu beneath.
- Stage hiding goes through `UiOverlayState.stageHidden` (no UiAction exists) — request 2.
- Edge overlay Auto cannot be sent yet — request 1.

## Remaining
- Device: legibility screencaps (22/18/14 px, both eyes, Player follow), HUD invalidations ≤ 1 Hz
  (`OverlayViews.updates`), T-5MIN, L-5 — need the integrated build and the glasses.

## 2026-09-22: merged main (contracts-v1.1, WP11 fixtures, WP7 MeshBuilder)
- Merge clean, no conflicts. No @Ignore markers in WP10 tests.
- `tools/gw :core:test :app:compileDebugKotlin`: BUILD SUCCESSFUL; core 154 tests, 0 skipped, 0 failed
  (WP10: GestureTableTest, GestureRulesTest, MenuTest, FirstRunTest, HudTest, CardsTest all green).
- contracts-v1.1 still has no Auto edge overlay in `SetSight` and no stage-hidden UiAction:
  requests 1 and 2 stay open; work-arounds unchanged. docs/wiring/WP10.md is current.
- WP complete apart from the device checks listed under Remaining (integrator, M6).

## 2026-09-22: review fixes
- A/V sync card (major): no live preview any more. Steps change only the card value; SetAvLead is emitted on tap only, cancel emits just SyncTest(false), so cancel stores nothing (§1.4). The card starts from the stored lead for the route key, else defaultLead(route) = 30. The measured Bluetooth default (§8.6) is not in UiFacts; requested from WP0/WP12 (docs/requests/WP10.md item 5). New CardsTest: Bluetooth, no stored key, step + cancel → no SetAvLead.
- Import panel: tap closes as in the §1.3 table. Rotation needs a confirmation: triple-tap arms it (the row reads "Tap now: rotate token"), then a tap rotates; closing disarms. Deviation: the arm gesture is triple-tap because swipes page the panel.
- KIT_PLAYABLE is only a re-render trigger; playable() reads kitStates[instrument] only.
- Source-credit timer never starts while the title shows; it starts at Enter (tap), ENTERED, MOVEMENT_STARTED or a new movementId after the title.
- Library shelves are built explicitly: Start here, Imported (if non-empty), Recently played (if non-empty), then the catalogue in order.
- Hint shows on facts.firstRun or sessions in 1..3.
- GestureTableTest now has CARD(SYNC), CARD(FLOOR), PANEL(IMPORT), PANEL(CREDITS) columns.
- Tests: tools/gw :core:test green (175 tests). AudioClockTest.sampleAllocatesNothing (not WP10) failed once under shared load and passed on rerun.
